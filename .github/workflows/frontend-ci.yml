name: Frontend CI

on:
  push:
    branches: ["**"]
    paths:
      - "apps/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "package.json"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    branches: ["**"]
    paths:
      - "apps/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "package.json"
      - ".github/workflows/frontend-ci.yml"

jobs:
  install-and-build:
    name: Install & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Get pnpm store directory
        id: pnpm-cache
        run: echo "pnpm_cache_dir=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.pnpm_cache_dir }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm -r --if-present run lint

      - name: Build
        run: pnpm -r --if-present run build

      - name: Test
        # tech-mobile excluded: React Native / Expo tests require a device
        # emulator not available in standard Linux CI runners
        run: pnpm -r --filter '!tech-mobile' --if-present run test

  e2e:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    needs: install-and-build
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Get pnpm store directory
        id: pnpm-cache
        run: echo "pnpm_cache_dir=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.pnpm_cache_dir }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers (chromium only for PR speed)
        if: github.event_name == 'pull_request'
        working-directory: apps/admin-web
        run: npx playwright install --with-deps chromium

      - name: Install Playwright browsers (all for main coverage)
        if: github.event_name != 'pull_request'
        working-directory: apps/admin-web
        run: npx playwright install --with-deps

      - name: Run E2E tests (chromium only for PR speed)
        if: github.event_name == 'pull_request'
        id: e2e
        working-directory: apps/admin-web
        run: npx playwright test --project=chromium
        continue-on-error: true

      - name: Run E2E tests (all browsers for main coverage)
        if: github.event_name != 'pull_request'
        id: e2e-main
        working-directory: apps/admin-web
        run: npx playwright test
        continue-on-error: true

      - name: Warn on E2E failure
        if: |
          (github.event_name == 'pull_request' && steps.e2e.outcome == 'failure') ||
          (github.event_name != 'pull_request' && steps.e2e-main.outcome == 'failure')
        run: echo "::warning::E2E tests failed — check the Playwright report artifact"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/admin-web/playwright-report/
          retention-days: 30
