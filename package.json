{
  "name": "office-hero-mobile",
  "version": "0.1.3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo 'lint step configured via pre-commit in CI'",
    "build": "pnpm --filter admin-web build",
    "test": "pnpm --filter admin-web test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
