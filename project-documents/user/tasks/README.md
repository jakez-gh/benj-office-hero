# Tasks

Each file here is a task breakdown for a slice (Phases 5–6).

Naming: `NNN-tasks.<name>.md` matching the slice index.

Each task should have:

- Clear action (e.g., "add `/login` route")
- File path hint
- Test hint
- Acceptance criteria
